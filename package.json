{
  "name": "beans-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @beans-web/web build && pnpm --filter @beans-web/server start",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix --max-warnings 0",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "codegen:check": "pnpm --filter @beans-web/web codegen && prettier --write apps/web/src/api/generated.ts && git diff --exit-code -- apps/web/src/api/generated.ts",
    "knip": "pnpm -r knip",
    "spell": "cspell \"**/*.{ts,tsx,md,json}\" --no-progress",
    "check:pins": "bash scripts/check-beans-pin.sh",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@eslint-community/eslint-plugin-eslint-comments": "^4.7.1",
    "cspell": "^9.0.0",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.0.1",
    "eslint-plugin-import-x": "^4.16.2",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "eslint-plugin-unicorn": "^63.0.0",
    "husky": "^9.1.7",
    "knip": "^6.26.0",
    "lint-staged": "^16.4.0",
    "prettier": "^3.5.3",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.24.1"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "prettier --write",
      "eslint --fix --max-warnings 0 --no-warn-ignored"
    ],
    "*.{json,md,yaml,yml}": [
      "prettier --write"
    ]
  },
  "prettier": "./.prettierrc.js",
  "pnpm": {
    "overrides": {
      "lodash": ">=4.18.0",
      "brace-expansion": ">=5.0.8"
    }
  }
}
