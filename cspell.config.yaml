version: "0.2"
language: en
words:
  - vitest
  - pnpm
  - tsconfig
  - commitlint
  - cspell
  - knip
  - eslint
  - prettier
  - husky
  - lcov
  - nocheck
  - noEmit
  - jsdom
  - typeof
  - devcontainer
  - codecov
  - typecheck
  - codegen
  - browsable
  - hmans
  - Bleve
  - handbellhub
  - testproj
  - refetches
  - refetched
  - WCAG
  - glassmorphism
  - Menlo
  - Consolas
  - affordances
  - dataviz
  - youruser
  - frontmatter
  - unstub
  - hhroot
  - romn
  - mousedown
  - srgb
  - ringoutmn
  - reparents
  - navigations
  - authelia
  - certresolver
  - letsencrypt
  - healthcheck
  - homelab
  - subfolders
  - tmpfs
  - websecure
  - GHSA
  - frvp
  - untaken
  - excludable
  - stmts
  - funcs
  - FNHSIO
  - dedup
  - subcomponents
  - unstarted
  - samebase
  - dedups
  - realpath
  - nosniff
  - clickjacking
  - noopener
  - noreferrer
  # cspell knows "viewport" but not its plural.
  - viewports
  - nosuchfield
  - basenames
  - unredacted
  - safelisted
  - bypassable
  # The errno a read-only container fails to boot with; named in SECURITY.md.
  - EROFS
  # The errno a bind-mount write fails with under a mismatched uid; README.md.
  - EACCES
  # cspell knows "reentrant" but not the noun.
  - reentrancy
  - hostnames
  - unparseable
  # Time-of-check-to-time-of-use; the class of race a symlink swap after
  # discovery validation exploits. cspell entries are case-insensitive, so
  # this one covers "toctou" too. Used across discovery/, beans/, and util/.
  - TOCTOU
  - repointed
  # The procfs file holding a process's argv. SECURITY.md names it as the
  # channel GraphQL variables remain readable through.
  - cmdline
  # The /proc mount option that hides other users' processes; named in
  # SECURITY.md as an operator mitigation for the same exposure.
  - hidepid
ignorePaths:
  - node_modules
  - dist
  # Untracked, per-contributor working notes; not part of the published docs.
  - docs/superpowers
  # Untracked security audit reports; local working artifacts, not published.
  - security
  - build
  - coverage
  - pnpm-lock.yaml
  - "*.min.js"
  - "*.min.css"
